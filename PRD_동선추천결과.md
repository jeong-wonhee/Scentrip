# PRD — AI 맞춤 동선 : 동선 추천 결과 화면

| 항목 | 내용 |
|---|---|
| 화면 ID | `viewRoute` |
| 선행 화면 | 동선 만들기 · 조건 입력 (`screens/route-conditions.html`) |
| 로직 참고 | `Scentrip_내여행_AI맞춤동선_통합_3.html` — 생성·이동시간·후보 정렬 알고리즘만 참고. 그 파일의 조건 설정 UI(사이드바 폼)는 위저드로 대체되어 쓰지 않는다 |
| 지원 환경 | **웹 전용 · 최소 폭 1280px** |
| 작성일 | 2026-09-01 (초안 2026-09-01 개정) |

---

## 1. 개요

조건 입력 위저드에서 `AI 맞춤 동선 추천 받기`를 누른 뒤 진입하는 **결과 화면**이다. AI가 구성한 일차별 코스를 타임라인으로 보여주고, 사용자가 그 자리에서 **편집(순서·시간대·일차·교체·추가·삭제)** 하고 **내 여행에 저장**할 수 있게 한다.

**조건 입력과 결과는 서로 다른 화면이다.** 조건은 카드형 위저드(720×848)가 단계별로 받고, 결과는 전체 폭 3분할 화면이다. 각각 자체 URL을 가진다.

레이아웃: `조건 사이드바 | 일차 타임라인 | 지도` 3분할. 사이드바는 위저드가 받아온 조건을 **표시하고 일부를 다시 바꿀 수 있는** 패널이며(§4.11), 위저드 DOM을 옮겨오지 않는다.

**뒤로가기는 두 층이다.**

| 위치 | 동작 |
|---|---|
| 위저드 1단계 | 위저드 진입 직전 화면 |
| 위저드 2단계 이상 | 이전 질문 단계 |
| 결과 화면 | 위저드 마지막 단계 (조건을 유지한 채) |
| 결과 화면 · 저장 안 된 변경 있음 | 이탈 확인 모달 먼저 (§4.12) |

---

## 2. 목적

1. **결과를 곧바로 신뢰 가능한 형태로 제시** — 왜 이 지역·이 장소인지(향 매칭률, 추천 사유)와 실제 이동 부담(구간별 이동 시간)을 함께 보여준다.
2. **재생성이 아니라 편집으로 수렴시킨다** — 마음에 들지 않는 한 곳 때문에 전체를 다시 뽑지 않도록, 장소 단위 교체·추가·이동 수단을 제공한다.
3. **저장으로 연결** — 편집한 동선을 `내 여행 > 내 동선`으로 넘겨 재방문 자산으로 만든다.

---

## 3. 배경 & 문제 정의

| # | 문제 | 결과 화면에서의 대응 |
|---|---|---|
| P1 | AI 추천은 "왜 이게 나왔는지" 근거가 없으면 신뢰되지 않는다 | 향 취향 매칭 배지 + 추천 이유 배너(한 줄 요약 / 상세는 툴팁) |
| P2 | 추천 코스 하나가 안 맞으면 전체를 버리게 된다 | 장소 단위 `장소 변경` 후보 3개 + `삭제` 후 빈자리 추천 |
| P3 | 일정표는 있으나 실제 이동 부담을 몰라 신뢰가 깨진다 | 모든 구간에 이동 수단·소요 시간 표시, 설정보다 먼 구간 경고, 구간별 외부 길찾기 연결 |
| P4 | 숙소가 정해지지 않으면 동선 계산 자체가 흔들린다 | 위저드에서 받은 숙소 주소를 거점으로 삼고, 미정인 박은 명시. 결과 화면에서 채우면 즉시 재계산 |
| P5 | 편집 결과가 저장되지 않고 유실된다 | 변경 감지(dirty) → 저장 바 상시 노출 + 이탈 시 확인 |

---

## 4. 기능 정의

### 4.1 진입 경로와 초기 상태

모드 값은 구현 기준으로 **`ai` / `region` / `picked`** 세 가지다. (구 명칭 `ai-region`·`fixed-region`·`wishlist`는 폐기)

| 진입 | 모드 | 위저드 | 잠금 |
|---|---|---|---|
| 홈 / 탐색 / 내 여행 의 `동선 만들기` → AI에게 여행지 추천 받기 | `ai` | 7단계 | 없음 |
| 같은 모달 → 원하는 지역의 동선 추천 받기 | `region` | 7단계 (지역 질문으로 시작) | 지역 고정 |
| 같은 모달 → 찜한 장소로 동선 추천 받기 → 장소 선택 화면 | `picked` | 6단계 (지역·출발지 질문 없음) | 지역·방식 잠금, 선택 장소는 첫 추천에 포함 |
| 장소 상세 > `이 장소로 동선 짜기` | `picked` (1곳) | 6단계 | 동일 |
| 내 여행 > 내 동선 카드 | 저장 시점의 모드 | **건너뜀 — 결과 화면 직행** | 지역·방식 잠금, 저장된 stops 그대로 복원 |
| 추천(큐레이션) 동선 | 큐레이션 기준 | 건너뜀 | 동일 |

- `picked` 모드에서 선택 장소는 **첫 추천에만 포함**되고 이후 고정되지 않는다(삭제·교체 가능). 삭제 시 시드에서 제거되어 재생성에도 되살아나지 않는다.
- 저장 동선 복원 시, 저장된 `day/slot` 정보가 있으면 그대로 배치하고 없으면 기간 템플릿에 순서대로 채운다.

**위저드가 넘겨주는 값**

| 키 | 질문 | 값 |
|---|---|---|
| `region` | 여행하실 지역은 어디인가요? | 시·도 17개 중 1 (`region` 모드만) |
| `nights` | 며칠동안 여행하실 계획인가요? | 당일치기 / 1박 2일 / 2박 3일 / 3박 4일 / 4박 5일 |
| `transport` | 주로 어떻게 이동하실 계획인가요? | 자동차 / 도보·대중교통 |
| `pace` | 일정은 어느 정도로 채우고 싶으신가요? | 넉넉하게 / 빽빽하게 |
| `distance` | 장소 사이 거리는 어느 정도까지 괜찮으신가요? | 가깝게 / 적당히 / 멀어도 괜찮아요 |
| `origin` | 출발지에서 첫 목적지까지 얼마나 이동하실 수 있나요? | 1·2·3시간 이내 / 상관 없어요 + 출발지 (`ai` 모드만) |
| `company` | 누구와 함께 여행하시나요? (선택) | 혼자 / 연인과 / 친구와 / 가족과 |
| `stay` | 어디에 숙박할지 정해지셨나요? (선택) | 박별 `미정` 또는 `주소` |

> **슬롯 템플릿 상한은 4박 5일(5일)이다.** 초안의 `5박 이상`은 숫자가 아니라 범위여서 일차 수를 정할 수 없었다. 선택지에서 제거했고, 템플릿은 당일치기 ~ 4박 5일 다섯 가지를 정의한다.

### 4.2 생성 처리

1. 유효성 검사
   - `picked`: 선택 장소 0곳 → 토스트 후 중단
   - `region`: 여행 지역 미선택 → 위저드에서 이미 막히므로 결과 화면에서는 발생하지 않음
2. 지역 확정
   - `ai`: 지역 랭킹 1위 선택
   - `region`: 선택 지역(향 데이터가 없으면 인접 대체 지역으로 fallback, 화면에 표기)
   - `picked`: 선택 장소들의 지역
3. **로딩은 결과 화면에서 보여준다.** 위저드의 마지막 CTA를 누르면 즉시 결과 화면으로 이동하고, 타임라인 영역에 로딩(약 0.85초)을 띄운다. 조건 사이드바와 지도 자리는 그대로 유지한다.
   - `ai`: "AI가 어울리는 지역을 고르는 중입니다"
   - 그 외: "AI가 향의 흐름을 그리는 중입니다"
   - 보조 문구: 향 매칭 → 이동 부담 → 운영시간 순 정렬 / 선택 장소 사이를 향과 거리로 채우는 중
4. 코스 생성 후 `dirty = false`로 초기화, 1일차 활성, 지도 동기화

**지역 선정 로직 (`ai`)**

```
score = 향 적합도 × 48
      + 숨은 지역 가산 12
      + 큐레이션 지역 가산 4
      + 안정적 난수 0~3            // 같은 조건에서 결과 재현
      + max(0, 10 − 이동시간(h) × 2.6)   // 출발지 있을 때만
```
- 이동 가능 시간 필터에 드는 후보가 없으면 **전체에서 가까운 순으로 완화**하고, 완화 사실을 추천 사유에 명시한다.
- `다른 지역으로 다시 추천`은 랭킹 다음 순위로 순환한다. 마지막에 도달해 1위로 돌아갈 때는 **`추천할 지역을 한 바퀴 돌았어요`를 토스트로 알린다.**

**코스 생성 로직**

| 단계 | 규칙 |
|---|---|
| ① 슬롯 템플릿 | 기간별 일차·시간대(오전/오후/저녁) 골격. 정원 초과 시 `packed` 슬롯부터 제거 |
| ② 장소 선정 | 시드 장소 우선 포함 → 나머지는 `향 매칭 − 이동부담계수 × 기준점 거리` 상위순 |
| ③ 동선 정렬 | 향 매칭 최고 장소에서 출발하는 최근접 체인(`매칭 − 이동부담계수 × 직전 장소와의 거리`) |
| ④ 배치 | 슬롯 순서대로 일차에 배치, 시간대 순 정렬. 시드 장소는 항상 `core`로 승격 |

이동 부담 계수: 가깝게 2.4 / 적당히 1.0 / 멀어도 괜찮아요 0.28

### 4.3 결과 헤더

| 요소 | 내용 |
|---|---|
| 매칭 배지 | 노출 중인 전체 장소의 향 매칭 평균(%) |
| 기준 문구 | `{기준 향 계열} 기준 · {지역 전체명} 일대 · {지역 태그라인}` |
| 변경 배지 | 저장되지 않은 변경사항이 있을 때만 노출 |
| 추천 이유 배너 | `ai` 모드이고 지역·장소가 잠기지 않은 경우에만 노출. 한 줄: `{취향 향} 취향 · {지역} {N}시간`. 상세 사유는 툴팁(출발지 기준 소요, 숨은 지역 여부, 이동 조건 완화 여부) |
| 다른 지역으로 다시 추천 | 배너 내 버튼. 재추천 후 변경 상태로 표시(dirty) |

### 4.4 일차 탭 · 일차 요약

- 기간만큼 탭 생성. 보조 라벨: 1일차=도착일 / 마지막=마지막 날 / 중간=온전한 하루 / 당일치기=하루 코스
- 탭 전환 시 타임라인·지도·요약을 함께 갱신하고 타임라인 스크롤을 최상단으로 되돌린다.
- 요약 줄: `{N}일차 동선 요약` / `장소 N곳 · 총 이동 {합계}`
  - 숙소 미정 구간이 있으면 `숙소 입력 시 N개 구간 추가 계산`으로 대체 표기
  - 그 외에는 계산 기준과 이동 부담 설정을 함께 표기한다. **계산 기준은 이동 수단이 `자동차`면 자동차, `도보·대중교통`이면 그 두 수단 중 빠른 쪽이다.** (자동차와 대중교통을 섞어 비교하지 않는다)

### 4.5 타임라인 노드 구성

일차별 노드 순서:

| 일차 | 구성 |
|---|---|
| 당일치기 | 코스만 |
| 1일차 | 코스 → 숙소(1박) |
| 중간 일차 | 숙소(전날, 출발) → 코스 → 숙소(오늘, 복귀) |
| 마지막 날 | 숙소(전날, 출발) → 코스 |

- 집·터미널 등 **장거리 관문 노드는 만들지 않는다.**
- **시간대 구분선**: 시간대가 바뀌는 지점에 오전(09–12)/오후(12–18)/저녁(18–21) 구분선 삽입
- **이동 구간**: 노드 사이마다 수단 아이콘·이름·소요 분 표시. `도보·대중교통` 설정에서는 두 수단을 모두 계산해 빠른 쪽을 기준값으로 쓰고 다른 쪽을 보조 표기한다. 구간마다 외부 지도 길찾기(카카오맵) 링크 제공.
- **먼 구간 경고**: `가깝게`에서 35분 초과, `적당히`에서 70분 초과 시 "이동 부담 설정보다 멀어요" 표기. `멀어도 괜찮아요`에는 경고가 없다.
- **미계산 구간**: 해당 박의 숙소가 미정이면 이동 구간 대신 안내 문구

**이동 시간 계산 (프로토타입 기준)**

| 파라미터 | 값 |
|---|---|
| 도로 우회 계수 / 보행 우회 계수 | 1.3 / 1.15 |
| 속도 | 자동차 40km/h, 대중교통 18km/h, 도보 4.3km/h |
| 대중교통 부대 시간 | 14분(대기+정류장 접근) |
| 도보 후보 제외 기준 | 75분 초과 |
| 표기 단위 | 5분 올림, 최소 5분 |

> 좌표 기반 추정치다. 실서비스에서는 경로 API 대체가 전제이며, 이 화면의 표시 규칙(빠른 수단 선택, 5분 단위, 먼 구간 경고)은 그대로 유지한다.
> **추정치 표기**: 구간 시간 앞에 `약`을 붙이고, 일차 요약 아래에 각주 한 줄(`이동 시간은 좌표 기반 추정치예요`)을 둔다. 구간마다 경고 문구를 반복하지 않는다.

### 4.6 장소 카드

표시: 이미지, 장소명, **향 매칭 %**, 한 줄 소개, 향 태그. 카드 본문 클릭 시 장소 상세로 이동.

액션:

| 액션 | 동작 | 결과 |
|---|---|---|
| 위로 / 아래로 | 인접 항목과 순서·시간대 교환 | 재계산 + 토스트, dirty |
| 시간대 변경 | 오전/오후/저녁 선택 후 시간대 순 재정렬 | 재계산 + 토스트, dirty |
| 일차 이동 | 다른 일차로 이동(기간 2일 이상일 때만) | 원래 일차에 **빈자리** 발생, dirty |
| 장소 변경 | 교체 후보 3개 패널 열기 | 4.7 |
| 삭제 | 확인 모달 → 제외 | 빈자리 발생, 시드에서도 제거, dirty |

- 패널(변경/추가)은 **동시에 하나만** 열린다.
- 일정 밀도가 `넉넉하게`이면 `core` 항목만 노출되고 `packed` 항목은 숨는다. 편집 인덱스는 항상 **노출 중인 항목 기준**으로 동작한다.
- **숨김을 명시한다.** 가려진 항목이 있으면 그 자리에 `넉넉한 일정에서 N곳이 가려졌어요 · 모두 보기`를 인라인으로 표시한다. 삭제로 오해되지 않게 하기 위한 장치다.

### 4.7 장소 변경 추천

**후보 조건 (필터)**
- 동일 여행 지역
- 해당 시간대에 운영(운영시간 정보가 없으면 통과)
- 현재 동선에 미포함
- 비슷한 역할/카테고리 (카페·식사·바다·자연·문화·전망·풍경·명소)
- **교체 후 앞뒤 구간이 §4.5의 먼 구간 경고 기준을 넘지 않을 것** — `가깝게 35분` / `적당히 70분` / `멀어도 괜찮아요`는 상한 130분
  - 조건을 만족하는 후보가 3개 미만이면 임계치를 1.6배까지 완화한 뒤 나머지 순으로 채운다.
  - **완화로 들어온 후보에는 카드에 `교체하면 이 구간이 설정보다 멀어져요`를 표시한다.**

> 초안에서는 이 필터가 `추가 이동 시간`(가깝게 45 / 적당히 80)을 기준으로 삼았다. 경고 기준(35 / 70)보다 느슨해서 **AI가 보여준 후보를 골랐는데 그 자리에 바로 경고가 붙는** 조합이 나왔다. 재는 대상을 `구간 절대 시간`으로 바꾸고 경고 기준에 맞췄다.

**정렬 가중치**

| 항목 | 비중 |
|---|---|
| 타입·테마 적합도 | 40% |
| 추가 이동시간·연결성 | 30% |
| 시간대·운영시간 적합도 | 20% |
| 평점·인기도 | 10% |

**기준 타입**: 현재 사용자 타입이 아니라 **이 동선이 처음 추천될 때의 타입·테마**를 쓴다. 저장 동선을 나중에 편집해도 그때의 기준이 유지된다.

**후보 카드 표시**: 이미지, 장소명, 평점·리뷰 수, 향 태그 2개, `{기준} 매칭 %`, 추가 이동시간(`추가 이동 +N분` / `이동 −N분` / `추가 이동 없음`), 변경하기 버튼.
→ **가중치·점수 등 원시 계산값은 노출하지 않는다.**

교체 후보가 없으면: "이 지역의 추천 장소가 이미 모두 일정에 포함되어 있어요."

### 4.8 빈자리 · 장소 추가

- 생성 시점의 일차별 노출 장소 수를 **기준선(quota)** 으로 저장한다. 삭제·일차 이동으로 그 아래로 내려간 만큼 **빈자리 노드**가 생긴다.
- 빈자리는 **자동으로 채우지 않고**, 사용자가 열었을 때만 후보를 계산한다.
- **빈자리는 한 번만 권한다.** 사용자가 빈자리를 닫으면 그 일차의 기준선을 현재 개수로 갱신하고 빈자리를 없앤다. 의도적으로 줄인 일정에 같은 제안이 계속 따라붙지 않게 하기 위해서다.
- 후보 산출은 4.7과 동일 로직에 `역할 제한 해제`를 적용하고, 기준점은 해당 일차의 마지막 장소(없으면 지역 중심), 다음 지점은 그날의 숙소로 둔다.
- 3개씩 노출, `다른 추천 보기`로 순환 페이징. 후보가 3개 미만이어도 순환해 3장을 채운다.
- 추가 시 마지막 장소와 같은 시간대로 배치 후 시간대 순 재정렬, dirty.

### 4.9 숙소

- **권역이 아니라 실제 숙소 주소를 다룬다.** 위저드가 박별로 `미정` 또는 `주소`를 받고, 결과 화면은 **그 주소의 좌표를 그대로 거점으로 삼아** 전후 구간을 계산한다.
- 기간 2일 이상일 때 박 수만큼 숙소 노드가 생긴다(`N박 · 출발/복귀`).
- 타임라인 노드에서 주소를 입력·수정할 수 있고, 조건 사이드바의 숙박 항목과 **동일 값을 공유**한다.
- 입력 즉시 **전후 구간을 재계산**한다.
- 미정 허용. 미정 상태에서도 동선은 생성되며, 해당 구간만 미계산으로 표기한다.
- 지역별 입력값을 보존해 지역·모드를 오갔다 돌아와도 유지한다.

### 4.10 지도

- 해당 일차의 노드만 표시. 장소는 순번 마커, 숙소는 별도 마커.
- 노드 순서대로 경로선 연결, 전체가 보이도록 자동 맞춤(`이 일차 동선 보기`로 재실행).
- **타임라인 ↔ 지도는 양방향으로 연동한다.**
  - 지도 마커 클릭 → 타임라인의 해당 카드로 스크롤·강조
  - 타임라인 카드 호버·포커스 → 지도의 해당 마커 강조 (내 여행 화면의 핀 호버 카드와 같은 상호작용을 반대 방향으로)
- 안내 문구: 위치 확인용이며 실제 길안내는 구간별 외부 링크 사용.
- 지도 로드 실패 시 대체 안내를 표시하고 나머지 기능은 그대로 동작한다.

### 4.11 조건 변경 (결과 화면에서의 재생성)

사이드바는 위저드가 받은 8개 조건을 모두 보여준다. 반영 방식은 셋으로 갈린다.

| 조건 | 결과 화면에서의 동작 |
|---|---|
| 여행 기간 | **즉시 재구성**, 1일차로 이동, 숙박 슬롯 재조정, dirty |
| 장소 사이 이동 부담 | **즉시 재구성**, dirty |
| **동행** | **즉시 재구성**, dirty |
| **숙소** | **즉시 재구성** — 거점이 바뀌면 코스 자체가 달라지므로 전후 구간 재계산에 그치지 않는다, dirty |
| 일정 밀도 | 재구성 없이 노출 범위 변경 + quota 갱신, dirty |
| 현지 이동 수단 | 재구성 없이 이동 시간만 재계산, dirty |
| 출발지 / 이동 가능 시간 / 지역 / 생성 방식 | 즉시 반영하지 않음 — **`이 조건으로 다시 만들기`** 를 눌러야 반영 |

- 출발지 미선택 시 이동 가능 시간은 `상관없어요`로 잠긴다.
- 생성 방식 변경 모달은 저장되지 않은 변경사항이 있으면 이탈 확인을 먼저 띄운다.

### 4.12 저장

- **변경 감지**: 위 편집·조건 변경이 발생하면 dirty. 결과 화면에는 저장 바가 항상 노출되며 문구만 상태에 따라 바뀐다.
- **저장 다이얼로그**: 제목 필수, 최대 30자, 기본값 `{지역} {기간} 향기 동선`. 빈 값이면 저장 버튼 비활성.
- **저장 모드**: 저장된 동선을 편집 중이면 `덮어쓰기` / `새 동선으로 저장` 둘 다 제공, 그 외에는 `저장`만.
- **저장 완료**: 완료 상태로 전환하고 `계속 편집` / `내 여행에서 보기` 제공.
- **이탈 가드**: dirty 상태에서 화면 이탈·방식 변경·브라우저 뒤로가기 시 확인 모달(`저장하기` / `저장하지 않고 나가기`).

**저장 payload**

| 그룹 | 항목 |
|---|---|
| 식별 | routeId, title, desc, sourceMode (`ai` / `region` / `picked`) |
| 지역·조건 | regionKey, days, transport, pace, spread, companions, reach, origin |
| 숙소 | `lodging[]` — 박별 배열 `{night, status: 미정\|입력, address, lat, lng}`. 요약 표시용으로 1박째를 따로 두더라도 **복원은 이 배열로 한다** |
| 코스 | stops[{id, name, lat, lng, scent, day, slot, pace}] |
| 요약 | 총 소요 시간, 대표 향 태그(최대 2), 평균 매칭률, 대표 이미지 |
| 추천 기준 | recommendationBasisType/Id/Label, recommendationTags |
| 참고 | mandatoryPlaceIds (첫 추천에 쓴 장소 · 잠금 아님) |

### 4.13 상태·예외

| 상태 | 처리 |
|---|---|
| 생성 중 | 타임라인 영역 로딩 오버레이, 조건 사이드바·지도 자리는 유지 |
| 해당 일차 장소 0곳 | "이 날은 비어 있어요" + 밀도/기간 조정 안내 |
| 교체·추가 후보 0개 | "이 지역의 추천 장소가 이미 모두 일정에 포함되어 있어요." |
| 이동 조건 내 지역 없음 | 전체 후보로 완화하고 추천 사유에 완화 사실 명시 |
| 지역 향 데이터 없음(`region`) | 인접 지역으로 대체하고 대체 사실 표기 |
| 숙소 미정 | 관련 구간 미계산 표기, 요약에 미계산 구간 수 표시 |
| 지역 재추천 한 바퀴 | `추천할 지역을 한 바퀴 돌았어요` 토스트 |
| 지도 로드 실패 | 대체 문구, 나머지 기능 정상 |
| 모든 편집 액션 | 토스트로 "무엇이 바뀌었고 다시 계산했다"를 알린다 |

---

## 5. 시나리오

**S1. AI 지역 추천 → 한 곳만 교체 후 저장**
1. 위저드에서 출발지·기간·이동 부담을 정하고 마지막 CTA → 결과 화면으로 이동, 타임라인 자리에 로딩 → 지역이 자동 선정되고 추천 이유 배너 노출
2. 2일차 오후 장소가 마음에 들지 않아 `장소 변경` → 후보 3개 중 하나로 교체
3. 이동 시간이 재계산되고 변경 배지 노출 → 제목 입력 후 저장 → 내 여행에서 확인

**S2. 찜한 장소로 시작**
1. 내 여행에서 찜한 장소 3곳 선택 → 위저드 6단계(지역·출발지 질문 없음)
2. 생성 → 선택 장소가 모두 포함되고 사이사이가 향·거리 기준으로 채워짐
3. 한 곳 삭제 → 빈자리 노드 생성 → `이 일차에 장소 추가`로 다른 후보 선택. 채우지 않고 닫으면 그 일차 기준선이 줄어 빈자리가 사라진다

**S3. 지역이 안 맞을 때**
1. 결과를 보고 지역 자체가 내키지 않음 → `다른 지역으로 다시 추천`
2. 랭킹 다음 지역으로 코스 전체 재구성, 변경 상태로 표시
3. 한 바퀴를 다 돌면 토스트로 알리고 1위로 회귀

**S4. 숙소 반영**
1. 위저드에서 1박째를 `미정`으로 두고 생성 → 숙소 전후 구간은 미계산 표기
2. 타임라인의 숙소 노드에 주소 입력 → 그 좌표를 거점으로 코스 재구성, 일차 요약의 총 이동 시간 갱신

**S5. 저장한 동선 재편집**
1. 내 여행 > 내 동선 카드 → 위저드를 건너뛰고 결과 화면 직행
2. 장소 교체 시 후보는 **저장 당시의 추천 기준**으로 계산됨
3. `덮어쓰기` 또는 `새 동선으로 저장` 선택

---

## 6. 성공 지표

| 구분 | 지표 | 정의 / 기준 |
|---|---|---|
| 핵심 | **동선 저장 전환율** | 결과 화면 도달 세션 중 1회 이상 저장한 비율 |
| 핵심 | **편집 후 저장율** | 편집(dirty) 발생 세션 중 저장으로 끝난 비율 — 이탈 가드에서 버리고 나간 비율의 역지표 |
| 신뢰 | **조건 재생성률** | `이 조건으로 다시 만들기` 실행 횟수 / 세션. **낮을수록 좋음** — 첫 결과가 조건을 못 맞췄다는 신호 |
| 탐색 | **지역 탐색 횟수** | `다른 지역으로 다시 추천` 실행 횟수 / 세션. **높낮이로 좋고 나쁨을 판단하지 않는다.** 저장 전환율과 함께 본다 — 두어 번 둘러본 뒤 저장했다면 성공 |
| 편집 | **장소 변경 후보 채택률** | 변경 패널을 연 횟수 대비 실제 교체 비율 (후보 품질 대리 지표) |
| 편집 | **빈자리 채움률** | 빈자리 발생 대비 장소 추가로 채운 비율 |
| 완결성 | **숙소 입력률** | 2일 이상 동선 중 최소 1박 주소를 입력한 비율 |
| 후속 | **저장 동선 재방문율** | 저장 후 7일 내 저장한 동선을 다시 연 비율 |
| 이탈 | **결과 화면 즉시 이탈률** | 결과 도달 후 상호작용 없이 이탈 (추천 품질/신뢰 경고 지표) |

보조 관측: 일차 탭 전환 수, 구간별 길찾기 링크 클릭률, 평균 매칭률과 저장 여부의 상관.

---

## 7. Out of Scope

- **실제 경로/시간표 연동** — 현재 이동 시간은 좌표 기반 추정치. 도로·철도·항공 API 연동은 별도 과제.
- **숙소 예약** — 주소 입력까지만 다루고 예약·요금은 제외한다. (개별 숙소 선택 자체는 이제 범위 안이다)
- **날짜 지정 일정** — 기간(N박)만 다루고 실제 캘린더 날짜, 요일별 휴무 반영은 제외.
- **실시간 운영시간·휴무·혼잡도 반영** — 시간대 적합도만 계산한다.
- **동선 공유 / 동행자 협업 편집**
- **드래그 앤 드롭 순서 변경** — 버튼·선택 기반 편집만.
- **일차 추가/삭제** — 기간 조건 변경으로만 조정된다.
- **예산·식비 등 비용 계산**
- **모바일·태블릿 레이아웃** — 웹 전용(최소 1280px)이므로 정의하지 않는다.

---

## 8. 결정 기록

| # | 이슈 | 결정 |
|---|---|---|
| D1 | 좁은 화면 대응 | **폐기** — 웹 전용, 최소 1280px |
| D2 | 이동 시간 신뢰도 표기 | 구간에 `약` 한 글자, 요약 아래 각주 한 줄. 구간마다 반복하지 않음 (§4.5) |
| D3 | 저장 payload의 숙박 정보 | `lodging[]` 박별 배열로 보존, 복원도 배열 기준 (§4.12) |
| D4 | 밀도 전환 시 숨겨지는 항목 | 가려진 자리에 `N곳이 가려졌어요 · 모두 보기` 인라인 표기 (§4.6) |
| D5 | 빈자리 기준선(quota) | 한 번만 권하고, 닫으면 기준선을 현재 개수로 갱신 (§4.8) |
| D6 | 재추천 순환의 끝 | 한 바퀴 돌면 토스트로 알림 (§4.2, §4.13) |
| D7 | 조건 설정 UI | 위저드 유지. 결과 화면은 별도 URL (§1) |
| D8 | 숙박 단위 | 권역이 아니라 **실제 숙소 주소**, 그 좌표를 거점으로 사용 (§4.9) |
| D9 | 교체 후보 임계치 | 후보 필터를 경고 기준(35 / 70)에 맞춤. 완화로 들어온 후보는 카드에 경고 표기 (§4.7) |
| D10 | 로딩 위치 | 결과 화면 타임라인 영역 (§4.2) |
| D11 | 기간 상한 | `5박 이상`을 없애고 `4박 5일`까지. 일차 템플릿도 5일까지 정의 (§4.1) |
| D12 | 지도 | Leaflet + OSM 타일로 통일. 내 여행·장소 선택의 그림 지도도 함께 교체 (§4.10) |

## 9. 남은 결정 사항

| # | 이슈 | 비고 |
|---|---|---|
| N2 | 사이드바에서 숙소를 바꿀 때의 재구성 범위 | §4.11에서 즉시 재구성으로 정했으나, 편집한 코스가 통째로 날아가는 것을 사용자가 어떻게 받아들일지 확인 필요. 확인 모달을 둘지 |
